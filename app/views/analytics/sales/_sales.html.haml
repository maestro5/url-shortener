%table.table.table-sortable.table-hover
  %thead
    %tr
      %th= sortable 'first_name'
      %th Last Name
      %th Email
      %th= sortable 'total_transactions'
      %th= sortable 'total_amount'
      %th= sortable 'average_amount'
      %th= sortable 'average_period', 'Average Period (days)'
  %tbody
    = render sales
